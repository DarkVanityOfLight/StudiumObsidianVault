
How do we decompose an arbitrary LIRA formula into following formed atoms:
- $x = 0$
- $x = 1$
- $x+y = z$
- $x < 0$
- $x < \lfloor y \rfloor$

----

Decompose $c$ into $c_{real}$ $c_{int}$
$c_{int} = \text{Binary decomposition}$
$c_{real} = \text{Binary decomposition}$
Construct $c_{new} = c_{real} + c_{int}$




Bring Atom into normal forms
$a_1 ?\lfloor x_1 \rfloor + ... +a_n ?\lfloor x_n\rfloor \sim  c, \qquad \sim\in\lbrace <, <=, =\rbrace$

Apply constant decomposition to $c$





