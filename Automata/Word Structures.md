

A word structure is the structure
$$(dom(w), \le, (P_a)_{a\in\Sigma})$$
where $dom(w)$ is the set of positions over $w$, and $P_a(i)$ is a predicate asserting that at position $i$ the word contains letter $a$

